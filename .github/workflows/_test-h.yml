name: test-h
on:
  workflow_call:
    inputs:
      runner:
        type: string
        default: ubuntu-latest
      enable-something:
        type: boolean
        default: false
permissions: {}
concurrency:
  group: test-${{ github.run_id }}
jobs:
  test:
    runs-on: ${{ inputs.runner }}
    steps:
      - run: echo "test-h passed"
