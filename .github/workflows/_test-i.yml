name: test-i
on:
  workflow_call:
    inputs:
      runner:
        type: string
        default: ubuntu-latest
      threshold:
        type: number
        default: 80
permissions: {}
concurrency:
  group: test-${{ github.run_id }}
jobs:
  test:
    runs-on: ${{ inputs.runner }}
    steps:
      - run: echo "test-i passed"
