# Test wrapper for OpenCI reusable-self-test.yml.
name: openci-self-test-rig

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/**"
  pull_request:
    paths:
      - ".github/workflows/**"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  call-self-test:
    uses: YiAgent/OpenCI/.github/workflows/reusable-self-test.yml@v3.1.17
    with:
      runner: ubuntu-latest
      enable-zizmor: true
      enable-pyflakes: true
