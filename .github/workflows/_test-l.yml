name: test-l
on:
  workflow_call:
    inputs:
      runner:
        type: string
        default: ubuntu-latest
permissions: {}
concurrency:
  group: dep-auto-merge-${{ github.event.pull_request.number }}
  cancel-in-progress: true
jobs:
  test:
    runs-on: ${{ inputs.runner }}
    steps:
      - run: echo "test-l passed"
