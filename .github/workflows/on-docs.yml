# test-rig on-docs — exercises reusable/docs.yml.
name: on-docs

on:
  pull_request:
    paths: ["docs/**", "**/*.md"]
  push:
    branches: [main]
    paths: ["docs/**", "**/*.md"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  docs:
    uses: YiAgent/OpenCI/.github/workflows/reusable/docs.yml@v3.0.0-rc1
    with:
      openci-ref: v3.0.0-rc1
      docs-dir:   docs
      site-dir:   site
